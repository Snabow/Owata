# WP-003 — Autonomous Structured Handoff Loop

**Status:** DESIGN APPROVED / SLICE 1 R2 BUILDER CANDIDATE
**Baseline:** Independent Review target R1 `a3be07aabe442c803e89b94ae7639113913bf103`
**Branch:** `wp-003/slice-1-control-protocol`
**Architecture decision:** `decisions/DEC-003-001-wp003-architecture.md`
**Execution Gateway decision:** `decisions/DEC-003-002-execution-gateway-pattern.md` (ACCEPTED; design-only — not implemented in Slice 1)
**Slice 1 evidence:** `evidence/wp-003-slice-1.md`
**Source of Truth:** `Project-OWATA-Genesis-Charter.md` — Phase 3 Role / Provider Separation
**Program Control decision:** OWATA-REQ-0020 / OWATA-REQ-0022 / OWATA-REQ-0024 / OWATA-REQ-0026

WP-003 itself is **not** DONE. Slice 1 R2 resolves OWATA-REQ-0025-F01/F02 under fake adapters only. Do not mark Independent Review PASS.

This document is the canonical Work Package for WP-003. A fresh agent reconstructs from repository records, not from chat history.

---

## Objective

Remove **Human Clipboard** from one complete Program Control → Builder → Independent Reviewer → Program Control cycle.

OWATA must persist structured handoffs, dispatch them through a deterministic Core, and resume after session loss, process interruption, or provider replacement — without Human copy/paste, manual agent invocation, manual role routing, or manual session switching.

Human involvement is allowed only at an intentional Human Gate.

---

## Why WP-003 exists

WP-002 completed the single-worker completion loop: a durable Control Core can take one queued work item from claim through verified COMPLETED (or classified FAILED) without Human intervention.

The remaining Genesis bottleneck is not local work execution. It is that Humans still operate as the message bus between logical roles:

```text
Program Control
↓ Human copy/paste + session switch
Builder
↓ Human copy/paste + session switch
Independent Reviewer
↓ Human copy/paste + session switch
Program Control
```

Charter Phase 3 success condition:

> Builder and Reviewer are separated by formal artifacts only, and REWORK returns to Builder automatically.

WP-003 is that phase, scoped to the smallest local implementation that proves OWATA semantics.

---

## Problem statement — Human Clipboard

Human currently:

- copies Work Packages and results between agents
- starts the next agent by hand
- chooses which session or product to open
- remembers where the cycle is
- routes REWORK back to Builder

That is operator work, not approval work. WP-003 must take it.

---

## Logical role invariants

Product names are not Control Core semantics. Logical roles are:

| Logical role | Current Champion (logical, replaceable) | Authority |
| --- | --- | --- |
| Program Control | ChatGPT | Meaningful next-action decisions |
| Builder | Cursor | Implementation within an approved Work Package |
| Independent Reviewer | Codex | Independent PASS / REWORK / BLOCK against artifacts |
| Human / Reality Boundary | Human | Goals, credentials, irreversible approval, Human Gate |

Do not encode ChatGPT, Cursor, Codex, Claude, ACP, OpenHands, or other provider names into Control Core state-machine semantics.

Current Champions remain until a later Program Control Decision changes them. Slice 1 uses fake adapters and therefore requires no provider-binding decision.

---

## In scope

- Versioned, machine-oriented structured envelopes as canonical inter-role communication
- Durable cycle / handoff state in SQLite
- Deterministic Dispatcher inside OWATA Core
- Provider-neutral adapter boundaries: `ProgramControlAdapter`, `BuilderAdapter`, `ReviewerAdapter`
- Capability / resource preflight sufficient to refuse an incapable runtime
- Failure classification that returns exhausted recovery to Program Control
- Reviewer trust boundary (exact SHA, isolated workspace, no Builder chat, no Reviewer source modification)
- Human Gate as the only intentional Human interruption
- Human Clipboard Zero as the central completion criterion
- Standing transition policy such as `on_builder_candidate = DISPATCH_REVIEW` only when persisted as Program Control policy

---

## Explicit non-goals

- Dispatcher, envelopes, adapters, schema v5, CLI integration, or provider integration in this canonicalization request
- Real LLM / provider setup in Slice 1
- Generalized provider routing / Model Router
- GitHub as a second authoritative workflow queue
- ACP or OpenHands as first implementation
- A new AI decision role called Runtime Operator
- Encoding “sessions do not exist”
- Automatic terminal project/work FAILED merely because a retry/rework counter is exhausted
- Installing or integrating headless Cursor, Codex, or other agent CLIs in this request
- Treating current `owata status` as authoritative project status
- Web UI / TFO / Phase 4 router

---

## Authority model

**SQLite** is authoritative for workflow / runtime / cycle / handoff state.

**Git** is authoritative for implementation artifacts, source, candidate SHA, Work Package documents, decisions, and durable evidence.

**GitHub** is remote Git / evidence durability and may later provide collaboration surfaces. It MUST NOT become a second authoritative workflow queue in WP-003.

Natural-language prompts are provider-adapter outputs, not canonical handoff state.

---

## Structured handoff

Canonical inter-role communication uses versioned, machine-oriented structured envelopes.

Minimum logical envelope families:

- Control Request
- Role Result
- Program Control Decision
- Human Gate / Human Gate Response

Exact field schema is implemented and tested in the first coding slice. Do not invent a second informal protocol in chat.

---

## Dispatcher boundary

Introduce a deterministic Dispatcher inside OWATA Core.

The Dispatcher may:

- persist validated envelopes
- inspect deterministic state
- execute predefined transitions
- perform capability preflight
- invoke configured adapters
- enforce leases / fencing / idempotency / retry budgets
- capture results
- resume interrupted work

The Dispatcher MUST NOT:

- originate Program Control decisions
- adjudicate Reviewer findings
- decide REWORK scope
- approve Builder output
- suppress or rewrite Reviewer findings
- silently change project architecture
- choose Human-impacting outcomes

Meaningful next-action decisions remain Program Control authority.

Automatic Builder-candidate → Reviewer dispatch is allowed only as execution of previously durable Program Control policy/decision. The Dispatcher does not independently decide that review should occur.

---

## Adapter boundary

Core architecture exposes provider-neutral logical adapters:

- `ProgramControlAdapter`
- `BuilderAdapter`
- `ReviewerAdapter`

Provider / model / runtime bindings remain replaceable.

Do **not** adopt “Program Control = `codex exec`” as an architectural decision. The Program Control adapter binding remains unresolved until real adapter integration.

An implementation runtime may transport or invoke roles. It receives no Program Control authority. Do not create a Runtime Operator decision role.

---

## Session is never authoritative

Session may exist. Session is never authoritative.

Agent / provider sessions may be created, reused, resumed, discarded, or replaced for cost or performance.

Correctness and project continuity must survive loss of every agent session.

A replacement runtime reconstructs required context from:

- SQLite durable state
- Git
- structured envelopes
- Work Package / Decision / Evidence records

B1/B2-style session labels are Genesis scaffolding only.

---

## Capability / resource contract

WP-003 introduces the minimum preflight needed to avoid assigning work to an incapable runtime.

The first protocol should be able to express requirements such as:

- `repository_read`
- `repository_write`
- `exact_checkout`
- `command_execution`
- `dependency_install` where applicable
- `network` access where applicable

Credential values MUST NOT enter Git, envelopes, or event logs.

Classify failure before execution where possible, for example:

- `CAPABILITY_BLOCK`
- `AGENT_UNAVAILABLE`
- `REPO_UNAVAILABLE`
- `CREDENTIAL_UNAVAILABLE`
- `RUNTIME_ERROR`
- `RESULT_INVALID`
- `RESULT_STALE`
- product / work failure

Do not implement generalized provider routing in WP-003.

---

## Failure semantics

Genesis Charter remains authoritative: **Failure is State, not Terminal.**

Do not define “rework budget exhausted → terminal FAILED” automatically.

When bounded automatic recovery is exhausted, control returns to Program Control.

Program Control may then choose, according to current capabilities:

- RETRY
- bounded REWORK
- REDESIGN
- change runtime / provider when available
- HUMAN_GATE
- ABORT

Terminal project / work failure requires an explicit semantic terminal decision such as Program Control ABORT, not merely exhaustion of an arbitrary retry counter.

This does not rewrite WP-002’s local worker-loop `FAILED` contract for deterministic proving samples. WP-003 governs the structured-handoff cycle, not `sum_two` attempt terminalization.

---

## Builder retry safety

Do not assume at-least-once Builder invocation is automatically safe.

An LLM Builder may produce different effects on replay.

WP-003 must eventually isolate Builder dispatch attempts so that:

- stale attempts cannot become canonical results
- fenced ownership controls result acceptance
- exact candidate SHA identifies accepted output
- an old attempt finishing late cannot overwrite the active candidate
- attempt-specific worktree / branch isolation is preferred

Detailed implementation belongs in later WP-003 slices.

---

## Reviewer trust boundary

Current logical Champion remains Independent Reviewer = Codex.

Required semantics, independent of provider:

- exact candidate SHA
- isolated / disposable review workspace
- no Builder conversation history
- no Reviewer source modification
- Program Control adjudicates findings

Reviewer output is PASS / REWORK / BLOCK plus findings. Reviewer does not merge, implement, or suppress findings.

---

## Human Gate semantics

Human is Goal Setter + Approver + Exception Handler, not Message Bus.

A Human Gate is an intentional, durable pause for:

- credentials the agent cannot obtain
- irreversible or externally binding approval
- outcomes that change what the project is trying to finish
- cost / safety / legal constraints

Human Clipboard (copy/paste, manual invocation, manual routing, session switching) is not a Human Gate.

---

## CLI status is not Source of Truth

Current `owata status` is still a WP-000 bootstrap stub. It hardcodes:

```text
Project: OWATA
State: Genesis
Next: Bootstrap control core
```

That output MUST NOT be treated as authoritative project status. WP-002 is DONE; the next Work Package is WP-003.

Do not “fix” `src/cli.ts` in the canonicalization request. A durable-state-backed status view belongs to WP-003 or a later appropriate slice.

Authoritative reconstruction uses:

1. current repository `main` / this Work Package
2. `Project-OWATA-Genesis-Charter.md`
3. `decisions/`
4. `work-packages/` and `evidence/`

---

## Central completion criterion — Human Clipboard Zero

One complete cycle:

```text
Program Control
→ Builder
→ Independent Reviewer
→ Program Control
```

completes without Human:

- copy/paste
- manual agent invocation
- manual role routing
- manual session switching

The acceptance canary must include at least one Reviewer REWORK path before PASS.

---

## Slice 1 — Builder candidate

### R0 (Independent Review target)

`992a4d286586bc1eab0776fcc96e2ea79c0f1346` — OWATA-REQ-0023 disposition **REWORK**.

### R1

`56934e8…` implementation / review HEAD `a3be07a…` — OWATA-REQ-0025 disposition **REWORK** (F01 provenance forgeable; F02 PC RETRY stranded).

### R2 (this rework)

Slice 1 R2 Builder candidate against R1 review HEAD `a3be07a…`. Resolves OWATA-REQ-0025-F01 and F02. WP-003 overall remains **NOT DONE**. Do not mark Independent Review PASS.

- protocol `owata.handoff/1` with runtime validation (+ `retry_of_request_id`)
- schema v5 (additive `recovery_target_request_id`; no version bump)
- full policy provenance: ACCEPTED PC dispatch + exact `install_policy` match, revalidated on reopen
- PC semantic RETRY ≠ automatic same-`request_id` dispatch retry
- durable recovery target across PC ADJUDICATE and restart
- killed-child PC fence regression asserts the child’s exact dispatch/fence
- prior R0/R1 acceptance contracts retained; fake adapters only

See `evidence/wp-003-slice-1.md`. No real LLM / provider integration exists.

## Later slices

Still required for WP-003 completion: real adapter bindings (guided by DEC-003-002), Human Clipboard Zero with real roles, durable-state-backed `owata status`, and remaining Phase 3 surfaces.

---

## Future / deferred

- Real Program Control / Builder / Reviewer adapter bindings
- **Execution Gateway** per `DEC-003-002` (Champion hypothesis may be Cursor; Control Core stays provider-neutral; not in Slice 1)
- Headless Cursor evaluation (when the real Builder adapter slice begins)
- Codex (or successor) as a real Reviewer adapter
- ACP as Challenger for future adapter transport evaluation only
- OpenHands
- Generalized provider routing / capability map / quota-aware router
- Durable-state-backed `owata status`
- CLI integration of cycle status
- Web UI / TFO canary / Phase 4–7

---

## Reconstruction checklist for a fresh agent

A new agent should be able to answer all of the following from repository records alone:

1. WP-002 is DONE at `2b105800dc18879b789b3d05dec3f6fca52f219d`
2. WP-003 exists to remove Human Clipboard
3. WP-003 will implement structured envelopes + deterministic Dispatcher + replaceable adapters
4. Architecture decisions in `DEC-003-001` and `DEC-003-002` are ACCEPTED
5. Provider / runtime bindings remain intentionally unresolved; Slice 1 uses fakes; Execution Gateway is deferred
6. `owata status` is a stub, not project truth
7. Slice 1 R0 IR was REWORK; R1 addressed four findings then R1 IR was REWORK (F01/F02); R2 addresses F01/F02; WP-003 is still NOT DONE
